export interface IOrderHeader {
  id: string;
  email: string;
  firstName: string;
  lastName: string;
  address: string;
  suburb: string;
  phoneNumber: string;
  paymentToken: string;
}
